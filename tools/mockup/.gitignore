# Renders and their shading are regenerated artefacts, not source. scenes.json
# IS source -- it is the only record of where the canvases are.
out/
edges/
shading/
upload-backup-*.json
__pycache__/
scenes.stretched-*.json
scenes.preRefine-*.json
